"""Package data: SKILL.md and skill.yaml templates, organized by strictness level.

Loaded by `bbsctl new` via importlib.resources. Templates ship with the
wheel as package data (see pyproject.toml [tool.hatch.build.targets.wheel.shared-data]).
"""
